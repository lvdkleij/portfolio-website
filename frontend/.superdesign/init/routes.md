# Routes

Nuxt 4 single-page application with no `app/pages/` directory.

- `/` → `app/app.vue`
  - `AppHeader`
  - `ConversationWorkspace`
  - `SystemContextRail`

Hash targets `#top`, `#work`, and `#resume` are currently interaction placeholders rather than routes.
