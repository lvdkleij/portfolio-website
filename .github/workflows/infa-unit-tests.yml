name: "Terraform Unit Tests"

on:
  push:
    paths:
      - 'infrastructure/**'

permissions:
  contents: read

jobs:
  terraform-unit-tests:
    name: 'Terraform Unit Tests'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
    
