# Portfolio shared components
Scope: the image-only `/` landing page. Nuxt 4 / Vue 3, plain scoped CSS; no component library or Tailwind configuration. No shared UI primitives are rendered by this route. The old PortfolioAssistant is dormant and must not be extracted or reused. Asterra-only components are outside this design request.

